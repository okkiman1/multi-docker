sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t okkiman1/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true okkiman1/react-test npm test

after_success:
  - docker build -t okkiman1/multi-client ./client
  - docker build -t okkiman1/multi-nginx ./nginx
  - docker build -t okkiman1/multi-server ./server
  - docker build -t okkiman1/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push okkiman1/multi-client
  - docker push okkiman1/multi-nginx
  - docker push okkiman1/multi-server
  - docker push okkiman1/multi-worker
